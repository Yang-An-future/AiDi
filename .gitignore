node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
serviceAccountKey*.json
firebase-adminsdk*.json

# Real names/PII — never committed, only exists locally / on the server
# that runs the seed script.
scripts/roster-data.json
